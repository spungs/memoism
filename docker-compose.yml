services:
  postgres:
    image: postgres:16-alpine
    container_name: memoism-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: memoism
      POSTGRES_PASSWORD: memoism
      POSTGRES_DB: memoism
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U memoism -d memoism"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
