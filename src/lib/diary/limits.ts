/**
 * **한 번에** 올릴 수 있는 사진 수. **티어와 무관한 고정값** —
 * 티어 차별은 스토리지 용량 쿼터(src/lib/storage/quota.ts)로만 한다.
 *
 * 일기 총량이 아니라 **요청당** 상한이다. 하루=일기 1건 구조라 총량으로 걸면
 * 곧 "하루 10장"이 되어, 채팅으로 사진을 보내는 날엔 오전에 소진된다.
 *
 * 요청당으로 두는 근거: 상한을 남긴 이유가
 *   - AI 정리 경로의 사진은 전부 Gemini 입력이라 장수가 곧 호출 비용이고,
 *   - 직접 저장 경로는 Server Action 본문 4MB(next.config.ts)에 먼저 걸려
 *     "저장 중 문제" 같은 정체불명 실패가 나기 때문인데,
 * 둘 다 "한 번에 몇 장이냐"의 제약이다. 저장 총량은 용량 쿼터가 지킨다.
 *
 * 서버·클라이언트 양쪽에서 쓰므로 server-only를 붙이지 않는다.
 */
export const MAX_IMAGES_PER_REQUEST = 10;
